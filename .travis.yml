language: python
python:
  - 2.6
  - 2.7
install:
  - pip install nose coverage requests futures
script:
  - nosetests --with-coverage --cover-package=encore --tests=encore.events.tests.test_event_manager
notifications:
  email:
    - cwebster@enthought.com
