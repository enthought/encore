language: python
python:
  - 2.7
install:
  - pip install coverage requests futures
script:
  - nosetests --with-coverage --cover-package=encore
  - make --directory docs html
notifications:
  email:
    - cwebster@enthought.com
